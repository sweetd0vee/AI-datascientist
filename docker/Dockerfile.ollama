# Dockerfile для Ollama (опционально, если нужна кастомная сборка)
# Обычно используется официальный образ ollama/ollama
FROM ollama/ollama:latest

# Можно добавить кастомные модели или настройки
# COPY models/ /models/

EXPOSE 11434

# Ollama запускается автоматически при старте контейнера

